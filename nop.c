#include "monty.h"

/**
 * m_nop -A function that opcode nop - doesn’t
 * do anything.
 * @h: double pointer to header (top) of the stack.
 * @line_number: counter for line number of the file.
 *
 * Return: void.
 */
void m_nop(stack_t **h, unsigned int line_number)
{
	(void) h;
	(void) line_number;
}
